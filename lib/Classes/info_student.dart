class infoStudent {
  String First_name = '';
  String uid;
  String Profile_image = '';
  String Role = "";

  infoStudent(
      {required this.uid,
      required this.First_name,
      required this.Profile_image,
      required this.Role});
}

class student_role {
  //List<Map<infoStudent>> mem_role = {};

  //student_role(this.mem_role);
}
